# Analysis

Analysis will validate run bundles before computing per-run latency summaries,
goodput, CPU use, and run-level bootstrap intervals. Plotting should consume a
frozen dataset without changing it.

No measured results are included yet. Keep synthetic fixtures under tests and
never include them in a reported performance comparison.
